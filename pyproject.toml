[project]
name = "windsurf-cascade-intent-twin"
version = "0.0.1"
description = "Scaffold: Cascade Intent Twin (independent Windsurf lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
